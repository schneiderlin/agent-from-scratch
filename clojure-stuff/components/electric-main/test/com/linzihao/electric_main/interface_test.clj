(ns com.linzihao.electric-main.interface-test
  (:require [clojure.test :as test :refer :all]
            [com.linzihao.electric-main.interface :as electric-main]))

(deftest dummy-test
  (is (= 1 1)))
